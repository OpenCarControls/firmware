{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug: PC board (app-build)",
            "preLaunchTask": "Build: app-build (debug)",
            "program": "${workspaceFolder}/.app_build/target/debug/app-build",
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {
                "RUST_LOG": "info"
            }
        }
    ]
}
